import React, { component } from 'react';
import { connect } from 'react-redux';

class Review extends componenet{
    render(){
        return(
            <div>

            </div>
        );
    }
}

export default connect()(Review);